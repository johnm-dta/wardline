---
specimen_id: "PY-WL-009-TP-TF-shape-only"
description: "Taint-flow: semantic boundary without prior shape check — partial validation"
rule: "PY-WL-009"
category: "taint_flow"
fragment: |
  from wardline.decorators import validates_semantic
  
  def shape_validate(data):
      """Shape validator in a separate function — not visible to semantic boundary."""
      if not isinstance(data, dict):
          raise TypeError("expected dict")
      return data
  
  @validates_semantic
  def check_business_logic(data):
      """Semantic boundary has no local shape evidence before subscript access."""
      if data["priority"] > 100:
          raise ValueError("priority too high")
      return data
taint_state: "PIPELINE"
expected_rules:
- PY-WL-009
expected_severity: ERROR
expected_exceptionability: UNCONDITIONAL
expected_match: true
sha256: "6484ca64dd97f1246f391a7a7344c1ab00cda31533dd7501df3f954f2009a072"
verdict: "true_positive"
boundaries:
  - function: "check_business_logic"
    transition: "semantic_validation"
