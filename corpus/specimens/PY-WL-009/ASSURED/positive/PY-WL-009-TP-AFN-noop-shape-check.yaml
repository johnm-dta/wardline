---
specimen_id: "PY-WL-009-TP-AFN-noop-shape-check"
description: "Adversarial false negative: shape check is a no-op isinstance(data, object) — always True"
rule: "PY-WL-009"
category: "adversarial_false_negative"
fragment: |
  from wardline.decorators import validates_semantic
  
  @validates_semantic
  def check_constraints(data):
      """Shape check is bare isinstance(data, object) — result discarded, so not real validation."""
      isinstance(data, object)
      if data["priority"] > 100:
          raise ValueError("priority too high")
      return data
taint_state: "ASSURED"
expected_rules:
- PY-WL-009
expected_severity: ERROR
expected_exceptionability: UNCONDITIONAL
expected_match: true
sha256: "9c8f6148b3a69c06ba7c3baf20bf0e8c527435256a086263d6623cb876094a6e"
verdict: "true_positive"
boundaries:
  - function: "check_constraints"
    transition: "semantic_validation"
