---
specimen_id: "PY-WL-008-TP-AFN-no-rejection"
description: "Adversarial false negative: boundary looks validated but has no actual rejection path"
rule: "PY-WL-008"
category: "adversarial_false_negative"
fragment: |
  from wardline.decorators import validates_shape
  
  @validates_shape
  def check_input(data):
      """Looks like validation but never rejects — always returns data."""
      if not isinstance(data, dict):
          data = {"raw": data}
      return data
taint_state: "ASSURED"
expected_rules:
- PY-WL-008
expected_severity: ERROR
expected_exceptionability: UNCONDITIONAL
expected_match: true
sha256: "debc4a2f82cdb30878756c60c13dbd4a266e7afabafac5ddbddef243a1d44833"
verdict: "true_positive"
boundaries:
  - function: "check_input"
    transition: "shape_validation"
