---
specimen_id: "PY-WL-006-TP-AFN-audit-in-broad-handler"
description: "Adversarial false negative: audit call wrapped in helper but called inside broad handler"
rule: "PY-WL-006"
category: "adversarial_false_negative"
fragment: |
  def save_record(audit_log, store, record):
      """Audit call inside broad handler — if write fails, handler masks it."""
      try:
          store.write(record)
      except Exception:
          audit_log.emit_event("write_failed", record.id)
taint_state: "INTEGRAL"
expected_rules:
- PY-WL-006
expected_severity: ERROR
expected_exceptionability: UNCONDITIONAL
expected_match: true
sha256: "700469d61af592d5b0cb8685296fd1a3db5b32463412a7ee5483dc66fd6f00c8"
verdict: "true_positive"
