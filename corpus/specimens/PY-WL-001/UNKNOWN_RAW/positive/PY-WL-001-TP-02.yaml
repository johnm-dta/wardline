---
specimen_id: "PY-WL-001-TP-02"
description: "defaultdict(list) constructor fires PY-WL-001"
rule: "PY-WL-001"
fragment: |
  from collections import defaultdict

  def aggregate(records):
      groups = defaultdict(list)
      for r in records:
          groups[r["type"]].append(r)
taint_state: "UNKNOWN_RAW"
expected_rules:
  - "PY-WL-001"
expected_severity: "ERROR"
expected_exceptionability: "STANDARD"
expected_match: true
verdict: "true_positive"
